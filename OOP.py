#Class Inheritance mechanism provides multiple base classees,
# a derived class can override any methods of its base class or classes, 
#and a method can call the method of a base class with same name

#Objects can contain arbitratry and kinds of data.add
#Dynamic nature of pyython: they can be created ar runtimt en further changes as needed

